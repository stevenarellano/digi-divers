# To Run This Model

1. create a folder entitled `backflip_images` that will hold
    - `Yes` folder => images of backflips
    - `No` folder => images of non-backflips
2. run through the `model.ipynb` notebook
